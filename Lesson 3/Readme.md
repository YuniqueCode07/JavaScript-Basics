3a. Create the text 'My name is: ' as a string.<br>
3b. Create your name as a string.<br>
3c. Using concatenation, add the 2 strings from 3a and 3b together to create the text: 'My name is: ____' (replace ____ with your name).<br>
3d. At a restaurant, you order 1 coffee (5$) and 1 bagel ($3). Using math, calculate the total cost, and using concatenation, create the text: 'Total cost: $____' (replace ____ with the total you calculated).<br>
3e. Do the same thing as 3d, but use a template string and interpolation.<br>
3f. Display the text from 3e in a popup using alert(...);<br>
3g. At a restaurant, you order 1 coffee ($5.99) and 1 bagel ($2.95). Using math, calculate the total cost, and using concatenation, create the text: Total cost: $____ (replace ____ with the total you calculated; hint: calculate in cents to avoid inaccuracies).<br>
3h. Do the same thing as 3g, but use a template string and interpolation.<br>
3i. Display the text from 3h in a popup using alert(...).<br>
3j. Using a multi-line string, create the text from 3h and add a line of text underneath: Thank you, come again!
